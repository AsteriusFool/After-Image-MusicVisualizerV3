# Generated dependencies and build output
node_modules/
dist/
renderer/lib/

# Local logs and recordings
*.log
*.mp4
*.webm

# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Local Spotify app credentials — see config/spotify.example.json
config/spotify.json

# Local AI-coding-tool config/skills/agents — not part of the shipped app;
# some of this predates this rule and is already tracked (flagged separately,
# not touched here), but nothing further should get swept in by `git add -A`.
.claude/
.codex/
.agents/
.github/agents/
.github/hooks/
.github/skills/

# Impeccable design-hook per-developer/runtime state — config.json (the
# shared ignore-list with reasons) is real project config and stays tracked.
.impeccable/config.local.json
.impeccable/hook.cache.json
